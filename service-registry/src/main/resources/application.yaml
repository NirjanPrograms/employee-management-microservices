spring:
  application:
    name: service-registry

#Default port for eureka server
server:
  port: 8761

eureka:
  client:
#    Eureka server do not register itself
    register-with-eureka: false
#    Server does not need service list from itself
    fetch-registry: false
